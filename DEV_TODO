## 提示词优化

## 工具优化

[X] 优化todo tool，每次压缩后将todo list嵌入messages list（参考hermes的todo_tool.py的实现）；

## 上下文管理

- 每次传入LLM前对messages list进行一次normalize, 确保下次传入的messages list的前面内容保持一致（kv cache友好）；

## 流程优化

- read_file工具增加文件读取cache机制，如果读取的文件的指定区域内容没有变化，直接返回“file content unchanged”(减少不必要的上下文占用)，否则返回新的内容；
- read_file工具增加一个clear cache的功能，当触发了上下文压缩后，需要清空之前的缓存；
- 自动skill沉淀机制：当处理用户发送的消息的过程中，调用工具的次数大于指定数量，在这次消息处理完成后，将调用的工具和调用的参数记录下来，形成一个新的技能，并将这个技能加入到系统中（可以参考hermes的skill_manager.py的实现）;
- 记录一次会话调用LLM的次数；
- 当前执行tool call时默认为全部异步并行，需要分情况处理：如果当前调用的工具是一个需要等待结果的工具（比如read_file），则需要等待这个工具的结果返回后再继续执行后续的工具调用；如果当前调用的工具是一个不需要等待结果的工具（比如todo_tool），则可以直接继续执行后续的工具调用；

## Hook

- pre_api_request
- post_api_request

## 记忆系统

- 借鉴hermes，压缩前调用LLM将与用户相关的信息写入MEMORY.md和USER.md，写入后还原messages list;


## 个人助手

### memory/skill沉淀机制

```py
    _MEMORY_REVIEW_PROMPT = (
        "Review the conversation above and consider saving to memory if appropriate.\n\n"
        "Focus on:\n"
        "1. Has the user revealed things about themselves — their persona, desires, "
        "preferences, or personal details worth remembering?\n"
        "2. Has the user expressed expectations about how you should behave, their work "
        "style, or ways they want you to operate?\n\n"
        "If something stands out, save it using the memory tool. "
        "If nothing is worth saving, just say 'Nothing to save.' and stop."
    )

    _SKILL_REVIEW_PROMPT = (
        "Review the conversation above and consider saving or updating a skill if appropriate.\n\n"
        "Focus on: was a non-trivial approach used to complete a task that required trial "
        "and error, or changing course due to experiential findings along the way, or did "
        "the user expect or desire a different method or outcome?\n\n"
        "If a relevant skill already exists, update it with what you learned. "
        "Otherwise, create a new skill if the approach is reusable.\n"
        "If nothing is worth saving, just say 'Nothing to save.' and stop."
    )

    _COMBINED_REVIEW_PROMPT = (
        "Review the conversation above and consider two things:\n\n"
        "**Memory**: Has the user revealed things about themselves — their persona, "
        "desires, preferences, or personal details? Has the user expressed expectations "
        "about how you should behave, their work style, or ways they want you to operate? "
        "If so, save using the memory tool.\n\n"
        "**Skills**: Was a non-trivial approach used to complete a task that required trial "
        "and error, or changing course due to experiential findings along the way, or did "
        "the user expect or desire a different method or outcome? If a relevant skill "
        "already exists, update it. Otherwise, create a new one if the approach is reusable.\n\n"
        "Only act if there's something genuinely worth saving. "
        "If nothing stands out, just say 'Nothing to save.' and stop."
    )
```

- 用户会话轮数达到指定数量，且距离上次触发memory沉淀工具调用后到达一定次数，触发一次后台Agent调用(使用_MOMORY_REVIEW_PROMPT)，决定是否需要进行memory级别的沉淀，如果需要，则调用memory相关工具重新整理MEMOMORY.md(memory沉淀是基于用户对话轮次决定);
- skill沉淀触发条件有待商榷，如果满足条件，触发一次后台Agent调用(使用_SKILL_REVIEW_PROMPT)，决定是否需要进行skill级别的沉淀，如果需要，则调用skill相关工具新建一个skill;
- 同时满足memory和skill沉淀条件时，触发一次后台Agent调用(使用_COMBINED_REVIEW_PROMPT)，同时评估是否需要进行memory级别和skill级别的沉淀，如果需要，则分别调用相关工具进行整理;